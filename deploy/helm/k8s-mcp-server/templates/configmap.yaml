apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "k8s-mcp-server.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "k8s-mcp-server.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- include "k8s-mcp-server.config" . | nindent 4 }}